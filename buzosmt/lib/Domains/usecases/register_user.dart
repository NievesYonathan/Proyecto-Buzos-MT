// lib/domain/usecases/register_user.dart

import '../models/user_model.dart';

class RegisterUser {
  Future<void> call(User user) async {
    // Aquí iría la lógica de negocio para registrar un usuario.
    // Por ejemplo, validar los datos y luego llamar al repositorio.
  }
}